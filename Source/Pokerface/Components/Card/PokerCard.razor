
@if (Model != null)
{
    <div class="poker-card" style="background-image:url('@Model.ImageUrl'); visibility:@(Model.IsVisible ? "visible" : "hidden");)"></div>
}

@code {

    [Parameter] public PokerCardModel? Model { get; set; }
       
}
